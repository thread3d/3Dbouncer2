---
phase: 05-ui-integration
plan: 01
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - src/ControlMode.cs
  - src/PhysicsSimulator.cs
autonomous: true
requirements:
  - CTRL-03
  - CTRL-05
must_haves:
  truths:
    - ControlMode enum exists with Automatic, Manual, Mixture values
    - PhysicsSimulator accepts mode changes at runtime
    - Manual mode pauses automatic physics updates
    - Mode can be queried and set via public API
  artifacts:
    - path: src/ControlMode.cs
      provides: ControlMode enum definition
      exports: ["ControlMode"]
    - path: src/PhysicsSimulator.cs
      provides: Mode-aware physics simulation
      min_changes: 20
  key_links:
    - from: MainForm (will set mode)
      to: PhysicsSimulator.Mode setter
      via: public property
      pattern: "physics.Mode = ControlMode.Manual"
---

<objective>
Add ControlMode enum and integrate mode switching into PhysicsSimulator.

Purpose: Foundation for manual control - define the three control modes and enable PhysicsSimulator to respond to mode changes. Manual mode pauses automatic physics updates so user can directly control particle positions.
Output: ControlMode.cs file, updated PhysicsSimulator with mode support.
</objective>

<execution_context>
@C:/Users/threa/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/threa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@D:/projects/3Dbouncer2/src/PhysicsSimulator.cs

<interfaces>
From PhysicsSimulator.cs (current):
```csharp
public class PhysicsSimulator
{
    private bool _isRunning = true;
    public bool IsRunning { get; set; }
    public void Update(float deltaTime, ParticleData[] particles)
    public void StepSimulation(float dt, ParticleData[] particles)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ControlMode enum</name>
  <files>src/ControlMode.cs</files>
  <action>Create new file src/ControlMode.cs defining public enum ControlMode with values: Automatic, Manual, Mixture. Add XML documentation explaining each mode:
- Automatic: Physics runs without user input (particles bounce)
- Manual: User controls position/rotation directly, physics paused
- Mixture: Physics runs + user input applies as forces (nudge)

Use "public enum ControlMode" with proper namespace TextBouncer.
</action>
  <verify>
    <automated>grep -q "public enum ControlMode" src/ControlMode.cs && grep -q "Automatic," src/ControlMode.cs && grep -q "Manual," src/ControlMode.cs && grep -q "Mixture" src/ControlMode.cs</automated>
  </verify>
  <done>ControlMode.cs exists with all three enum values and documentation</done>
</task>

<task type="auto">
  <name>Task 2: Add mode support to PhysicsSimulator</name>
  <files>src/PhysicsSimulator.cs</files>
  <action>Modify PhysicsSimulator to support ControlMode:

1. Add "using TextBouncer;" if not present (for ControlMode)
2. Add private field: private ControlMode _currentMode = ControlMode.Automatic
3. Add public property Mode with getter/setter
4. In setter, update _currentMode and handle state changes:
   - If Manual mode: set _isRunning = false (pause physics)
   - If Automatic/Mixture: set _isRunning = true (resume physics)
5. Update Update() method to check mode:
   - If Manual: return early (no physics updates)
   - If Automatic/Mixture: run normal physics

Keep _isRunning for internal state but expose Mode as the primary control interface. Maintain backward compatibility - existing code using IsRunning should still work.
</action>
  <verify>
    <automated>grep -q "public ControlMode Mode" src/PhysicsSimulator.cs && grep -q "_currentMode" src/PhysicsSimulator.cs && grep -q "ControlMode.Manual" src/PhysicsSimulator.cs</automated>
  </verify>
  <done>PhysicsSimulator has Mode property, pauses physics in Manual mode, automatic/Mixture run physics</done>
</task>

<task type="auto">
  <name>Task 3: Add manual override fields and methods</name>
  <files>src/PhysicsSimulator.cs</files>
  <action>Add support for manual position/rotation overrides to PhysicsSimulator:

1. Add fields for manual overrides:
   - private Vector3 _manualPosition = Vector3.Zero
   - private Vector3 _manualRotation = Vector3.Zero (Euler angles in degrees: pitch, roll, yaw)

2. Add public properties:
   - public Vector3 ManualPosition { get; set; }
   - public Vector3 ManualRotation { get; set; }

3. Add method to apply manual overrides to particles:
   - public void ApplyManualOverrides(ParticleData[] particles, Vector3 position, Vector3 rotation)
   - This will be called from UI in Manual mode to set particle positions directly

4. For now, implement simple position offset - apply position to all particles uniformly (translation only). Rotation will be implemented in Phase 6 for Mixture mode full implementation.

Document that ApplyManualOverrides should only be called when Mode == Manual.
</action>
  <verify>
    <automated>grep -q "public Vector3 ManualPosition" src/PhysicsSimulator.cs && grep -q "ApplyManualOverrides" src/PhysicsSimulator.cs && grep -q "Vector3 _manualPosition" src/PhysicsSimulator.cs</automated>
  </verify>
  <done>PhysicsSimulator has ManualPosition, ManualRotation properties and ApplyManualOverrides method</done>
</task>

</tasks>

<verification>
- ControlMode enum compiles and is accessible from MainForm
- PhysicsSimulator.Mode can be set to Automatic, Manual, or Mixture
- Setting Mode to Manual pauses physics (IsRunning becomes false)
- Setting Mode to Automatic resumes physics
- ManualPosition and ManualRotation properties exist
- ApplyManualOverrides method exists and accepts particles array
</verification>

<success_criteria>
1. ControlMode.cs exists with Automatic, Manual, Mixture enum values
2. PhysicsSimulator has Mode property that controls automatic physics updates
3. Manual mode pauses physics simulation
4. Manual position/rotation properties exist for UI binding
5. PhysicsSimulator can apply manual overrides to particles
6. Code compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui-integration/05-01-SUMMARY.md`
</output>
