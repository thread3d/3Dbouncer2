---
phase: 05-ui-integration
task: 3
total_tasks: 4
status: blocked
last_updated: 2026-04-02 14:45 UTC
---

<current_state>
Phase 5 (UI Integration) is in progress. We're working on plan 05-02 (collapsible side panel layout) but have hit a critical blocker: particles are not rendering despite extensive debugging.

Current layout uses a simple Panel.Dock=Left for the side panel (removed SplitContainer due to OpenGL context issues). GLControl is added directly to the form.

Shader SSBO layout was fixed: ParticleData struct has 48 bytes (Position+Velocity+Color), shader now reads Position at idx*3+0 and Color at idx*3+2.

Still: NO PARTICLES VISIBLE.
</current_state>

<completed_work>

- Phase 1 (Foundation): Complete - OpenGL context, GLControl, basic rendering
- Phase 2 (Text-to-Particles): Complete - SkiaSharp rasterization, hole detection with TDD
- Phase 3 (Particle Rendering): Complete - Instanced rendering, SSBO, Camera with orbit controls
- Phase 4 (Physics Simulation): Complete - Fixed timestep physics, elastic box collisions
- Phase 5 Plan 01: Complete - ControlMode enum, PhysicsSimulator mode support
- Phase 5 Plan 02: In Progress - Side panel layout, BUT particles not visible
  * Simplified layout: Panel.Dock=Left for controls
  * Removed SplitContainer (was causing GL context issues)
  * Fixed shader SSBO layout (48 bytes per particle, skip Velocity)
</completed_work>

<remaining_work>

- Task: Debug why particles are not visible
  * Check camera distance from particle positions
  * Verify particle positions are in view frustum
  * Check particle size is large enough
  * Verify MVP matrix calculation
  * Try rendering a single test particle at origin
- Task: Complete side panel controls (position sliders, rotation sliders, mode switcher)
- Task: Add FPS counter and responsiveness validation
- Task: Implement translucent cube with opacity slider (new requirement)
</remaining_work>

<decisions_made>

- Replaced SplitContainer with simple Panel.Dock (SplitContainer interfered with OpenGL context)
- Fixed shader SSBO layout: ParticleData is 48 bytes (Position+Padding+Velocity+Padding+Color), shader reads Position at idx*3+0, Color at idx*3+2
- Side panel width set to 420px to fit TrackBar controls
- Color button now shows selected color with contrasting text
</decisions_made>

<blockers>

- CRITICAL: Particles not visible despite:
  * No OpenGL errors
  * Shader compiles successfully
  * SSBO layout fixed
  * Physics simulation running
  * No crashes

- Possible causes:
  * Camera position too close/far from particle positions
  * Particle positions in wrong coordinate space
  * Particle size too small (default 4.0f might be invisible)
  * MVP matrix calculation issue
  * Depth testing culling particles
  * Particle positions are z=0 (in a 3D box from -1 to +1), camera at distance=10
</blockers>

<context>
We have a complete particle system that SHOULD work:
- Text renders to bitmap with SkiaSharp
- Hole detection works (9/9 tests pass)
- Particles generate with positions normalized to -1 to +1
- Instanced rendering with SSBO uploads
- Camera with orbit/zoom/pan
- Physics simulation with fixed timestep

But particles are not visible. Need systematic debugging:
1. Check if box is visible (if yes, rendering pipeline works)
2. Try rendering particles at a known position (origin)
3. Check camera distance from particle cloud
4. Increase particle size significantly
5. Add debug output for particle positions

NEW REQUIREMENT: User wants a visible cube for the physics boundaries, with translucent far panel and opacity slider.
</context>

<next_action>
Start with: Check if the semi-transparent box is visible when running the app. If yes, the rendering pipeline works and issue is particle positions/size. If no, there's a deeper GL/rendering issue.

Then: Try adding a single large test particle at origin (0,0,0) with bright color to verify instanced rendering works.
</next_action>

<resume_command>
/gsd:resume-work
</resume_command>
